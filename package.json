{
  "name": "quickbite-food-ordering-system",
  "version": "1.0.0",
  "description": "QuickBite MERN Stack Food Ordering Application",
  "scripts": {
    "start": "concurrently \"npm --prefix server start\" \"npm --prefix client start\"",
    "dev": "concurrently \"npm --prefix server start\" \"npm --prefix client start\"",
    "server": "npm --prefix server start",
    "client": "npm --prefix client start",
    "seed": "npm --prefix server run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
